#!/bin/bash
# Seed initial data (not needed as feed-generator creates data automatically)

echo "DeltaStream uses live feed generator."
echo "Data is automatically generated once services start."
echo "No manual seeding required!"
